# TLA+
A language for describing a system as states and transitions, and a model checker (TLC) that explores every reachable state of a small model. It verifies the harness, not the story: no unvalidated chapter is published, resume never duplicates a chapter, old versions survive, retries are bounded, and every run terminates.
