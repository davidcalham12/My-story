# LLM-as-judge
A model scores another model's output against a rubric. It catches what counting cannot (tone, arc, natural personalisation) and it does not reproduce: the same book can score differently twice. So the judge gives a justification per criterion, a human reviews one novel with the same rubric, and the judge never has the last word on a rule a script can check.
