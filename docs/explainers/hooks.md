# Hooks
Commands Claude Code runs automatically on an event, whatever the model decides. storyMaker has two on every chapter written: validate-chapter (length, names, schema) and policy (forbidden words, audit log). A hook turns "the procedure says to check" into "the check always runs".
