# Harness
The code around a model that decides what it is given, checks what it returns and decides what happens next. Here Claude Code runs a written procedure, twelve agents each do one job with only the tools that job needs, and a gate refuses chapters until they pass. The model writes; the harness is what makes the output trustworthy.
