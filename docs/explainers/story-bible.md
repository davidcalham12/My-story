# Story bible
The single shared memory of the novel: world rules, characters, timeline, promises to the reader. Because the writer never sees earlier chapters, every fact a later chapter needs must be in the bible. storyMaker keeps it as Markdown the agents write and as SQLite rows a script extracts, so validators can ask which chapters use the dog's name.
