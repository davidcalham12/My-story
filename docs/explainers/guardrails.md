# Guardrails
Rules enforced in code on what goes in and out. Here: forbidden words at three levels (global, client, novel), normalised for case, accents and plurals, checked on every chapter; a hit sends the chapter back and is logged; the buyer's free text is stored as material and never followed as an instruction.
