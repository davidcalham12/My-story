# Observability
Being able to answer what happened and what it cost, after the fact. Every stream line is stored; every figure carries its provenance (measured, reported, reconstructed, estimated, absent); each novel becomes a Langfuse session with spans per agent and scores per validator. What cannot be measured is written as absent, never as zero.
