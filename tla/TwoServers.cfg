\* The sweep as written in backend/runs/service.py `sweep_orphans`.
\* Expected: TLC finds a violation of NoLiveRunMarkedHalted (red-team case 13).
SPECIFICATION Spec
CONSTANTS
  Servers = {s1, s2}
  Runs = {r1, r2}
  None = None
  GUARD = FALSE
  RESWEEP = FALSE
INVARIANTS
  TypeOK
  NoLiveRunMarkedHalted
  HaltedIsFinished
PROPERTIES
  StartLeavesNoOrphan
