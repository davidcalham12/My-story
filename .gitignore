__pycache__/
.venv/
node_modules/
*.db
*.db-wal
*.db-shm
.env
# The frontend build only. A bare `dist/` also matched output/<slug>/dist/, so
# every generated manuscript was silently untracked: the repo kept the rejected
# drafts, the critiques and the feedback sheets, and threw away the book.
frontend/dist/
.pytest_cache/

# TLC's working directory: model-checking output, not source.
tla/states/
