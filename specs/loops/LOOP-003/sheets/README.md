# Sheets sent, one file per attempt.

Written by the orchestrator as it sends each one. Empty until a run uses the
LOOP-003 procedure; every file here is a sheet a writer actually received, kept
verbatim so the loop can ask which phrasing produced a correction first time.
